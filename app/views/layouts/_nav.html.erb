<nav class="navbar">
  <ul class="nav navbar-nav">
   <% if user_signed_in? %>
      <li class="nav-item">
        <a class="nav-link"><%= link_to current_user.name, user_path(current_user) %></a>
      </li>
      <li class="nav-item">
        <a class="nav-link"><%= link_to "Log Out", destroy_user_session_path, method: :delete %></a>
      </li>
    <% else %>
      <li class="nav-item">
        <a class="nav-link">
          <%= link_to "Log In", new_user_session_path %>
        </a>
      </li>
    <% end %>
  </ul>
</nav>
